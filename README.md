# IPTV Max Roku App

This is a Roku SceneGraph IPTV app workspace based on the supplied IPTV Max design pages. The app supports backend-managed IPTV playlists, local/demo media, TV-first browsing, playback, account recovery, subscription scaffolding, feedback, favorites, and parental controls.

## Project Structure

- `source/main.brs` launches the Roku SceneGraph screen.
- `components/MainScene.xml` declares the main SceneGraph component.
- `components/MainScene.brs` owns IPTV Max screen routing and global navigation.
- `components/pages/` contains the TV screens for Home, playlists, Live TV, Movies, Series, Favorites, Settings, Profile, Subscription, Feedback, and playback.
- `components/shared/BackendApi.brs` and `components/tasks/BackendApiTask.brs` contain the backend API request/response helpers used by playlist sync and playback lookup.
- `manifest` contains Roku channel metadata and required libraries.
- `build/` is generated by BrighterScript and is not committed.

## Commands

- `npm.cmd run check` validates and packages the app locally.
- `npm.cmd run build` creates the Roku zip.
- `npm.cmd run deploy` deploys to a configured Roku device.

## Current Screens

- Home dashboard
- Add Playlist with M3U and Xtreme account modes
- My Playlists manager
- Live TV with paginated backend channel loading
- Series browser
- Movies browser
- Settings
- Favourites
- Profile and subscription/account recovery screens
- Feedback
- Player

## Backend Playlist Behavior

- Backend playlists keep local Roku card IDs separate from backend playlist IDs.
- Live TV, Movies, and Series load their relevant page data separately instead of relying on one mixed page payload.
- Large backend playlists use cursor pagination. The app renders the first page quickly, appends more rows near the end of the list, and avoids loading an entire 20,000-item playlist into memory at once.
- Page header counts use backend total metadata when available, such as `total_count`, `total`, `total_items`, `active_channel_count`, or `channel_count`. Pagination size such as `1000` is only the loaded page size, not the final displayed total.
- Reliable Live/Movie/Series separation depends on backend extraction preserving item-level `content_type` or `media_type`. The client also applies fallback classification hints, but backend metadata is the durable source of truth.

Real IPTV provider credentials, stream URLs, payment provider keys, and signing credentials must stay outside source control.
